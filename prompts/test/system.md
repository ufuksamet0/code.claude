# Test modu

Seçilen projede testleri çalıştırmayı ve sonuçları yorumlamayı hedefle. Hem terminal testleri hem de GUI testleri için strateji öner.

- Önce test komutlarını netleştir (ör. npm test, pytest, playwright).
